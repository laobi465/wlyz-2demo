' 极策k网络验证 易语言 接口规范
' 作者: 极策k  日期: 2026-07-21
'
' 本文件定义易语言模块的子程序签名规范。
' 易语言源码为文本格式（.e），以下采用易语言伪代码语法。
' 实际使用时需在易语言 IDE 中创建对应子程序并粘贴实现。

.版本 2

.程序集 极策k客户端

' ==================== 全局变量 ====================

.程序集变量 服务端地址, 文本型
.程序集变量 应用Key, 文本型
.程序集变量 签名密钥, 文本型
.程序集变量 RSA公钥, 文本型
.程序集变量 超时秒数, 整数型
.程序集变量 会话ID, 文本型
.程序集变量 心跳间隔, 整数型
.程序集变量 心跳线程句柄, 整数型
.程序集变量 心跳停止标志, 逻辑型
.程序集变量 连续失败次数, 整数型

' ==================== 初始化 ====================

.子程序 极策k_初始化, 逻辑型
' 初始化客户端，必须在使用其他接口前调用
' 返回：成功=True，失败=False
.参数 服务端URL, 文本型, , 如 https://api.jicek.example.com
.参数 应用Key, 文本型, , 开发者后台创建软件时生成
.参数 签名密钥, 文本型, , HMAC-SHA256 签名密钥
.参数 RSA公钥PEM, 文本型, , PEM 格式公钥
.参数 超时秒, 整数型, 可空, 默认 10

服务端地址 ＝ 服务端URL
应用Key ＝ 应用Key
签名密钥 ＝ 签名密钥
RSA公钥 ＝ RSA公钥PEM
超时秒数 ＝ 选择 (超时秒 ＝ 0, 10, 超时秒)
会话ID ＝ ""
心跳间隔 ＝ 60
连续失败次数 ＝ 0
心跳停止标志 ＝ 假

' 校验参数
如果真 (服务端地址 ＝ "" 或 应用Key ＝ "" 或 签名密钥 ＝ "" 或 RSA公钥 ＝ "") Then
    返回 (假)

返回 (真)


' ==================== 卡密验证 ====================

.子程序 极策k_验证卡密, 逻辑型
' 卡密验证（核心接口）
' 返回：成功=True，失败=False（参考参数返回详细信息）
.参数 卡密, 文本型
.参数 返回会话ID, 文本型, 参考
.参数 返回到期时间, 文本型, 参考, 可空
.参数 返回剩余次数, 整数型, 参考, 可空
.参数 返回功能列表, 文本型, 参考, 可空

.局部变量 指纹JSON, 文本型
.局部变量 指纹值, 文本型
.局部变量 卡密密文, 文本型
.局部变量 请求体, 文本型
.局部变量 响应, 文本型
.局部变量 响应码, 整数型

' 1. 采集设备指纹
指纹JSON ＝ 极策k_采集设备指纹 (RSA公钥)
指纹值 ＝ JSON_取字段 (指纹JSON, "fingerprint")

' 2. RSA 加密卡密
卡密密文 ＝ RSA_OAEP加密_Base64 (卡密, RSA公钥)

' 3. 构造请求体
请求体 ＝ "{""fingerprint"":""" + 指纹值 + """,""encryptedDetail"":""" + JSON_取字段 (指纹JSON, "encryptedDetail") + """,""cardCipher"":""" + 卡密密文 + """,""deviceName"":""" + JSON_取字段 (指纹JSON, "deviceName") + """,""osType"":""windows"",""clientVersion"":""jicek-sdk-epl-0.3.1"",""isVm"":" + JSON_取字段 (指纹JSON, "isVm") + ",""vmExtra"":""" + JSON_取字段 (指纹JSON, "vmExtra") + """}"

' 4. HTTP 请求
响应 ＝ 极策k_HTTP请求 ("POST", "/api/sdk/card/verify", 请求体, 指纹值)

' 5. 解析响应
响应码 ＝ 到整数 (JSON_取字段 (响应, "code"))
如果 (响应码 ≠ 200) Then
    极策k_处理错误码 (响应码, JSON_取字段 (响应, "msg"))
    返回 (假)

会话ID ＝ JSON_取字段 (JSON_取字段 (响应, "data"), "sessionId")
返回会话ID ＝ 会话ID
返回到期时间 ＝ JSON_取字段 (JSON_取字段 (响应, "data"), "expireTime")
返回剩余次数 ＝ 到整数 (JSON_取字段 (JSON_取字段 (响应, "data"), "remainCount"))
返回功能列表 ＝ JSON_取字段 (JSON_取字段 (响应, "data"), "features")

返回 (真)


' ==================== 心跳保活 ====================

.子程序 极策k_心跳, 逻辑型
' 单次心跳
' 返回：成功=True，失败=False
.参数 下次间隔秒, 整数型, 参考

.局部变量 指纹JSON, 文本型
.局部变量 指纹值, 文本型
.局部变量 时间戳, 文本型
.局部变量 随机串, 文本型
.局部变量 请求体, 文本型
.局部变量 响应, 文本型
.局部变量 响应码, 整数型

如果真 (会话ID ＝ "") Then
    返回 (假)

指纹JSON ＝ 极策k_采集设备指纹 (RSA公钥)
指纹值 ＝ JSON_取字段 (指纹JSON, "fingerprint")

时间戳 ＝ 到文本 (取毫秒时间戳 ())
随机串 ＝ 取UUID ()

请求体 ＝ "{""tenantId"":0,""softwareId"":0,""fingerprint"":""" + 指纹值 + """,""timestamp"":" + 时间戳 + ",""nonce"":""" + 随机串 + """}"

响应 ＝ 极策k_HTTP请求 ("POST", "/api/sdk/device/heartbeat", 请求体, 指纹值)

响应码 ＝ 到整数 (JSON_取字段 (响应, "code"))
如果 (响应码 ≠ 200) Then
    极策k_处理错误码 (响应码, JSON_取字段 (响应, "msg"))
    返回 (假)

心跳间隔 ＝ 到整数 (JSON_取字段 (JSON_取字段 (响应, "data"), "nextInterval"))
如果真 (心跳间隔 ＜ 5 或 心跳间隔 ＞ 300) Then
    心跳间隔 ＝ 60

下次间隔秒 ＝ 心跳间隔
返回 (真)


.子程序 极策k_启动心跳, 逻辑型
' 启动后台心跳线程
' 返回：成功=True

心跳停止标志 ＝ 假
心跳线程句柄 ＝ 启动线程 (&心跳线程入口, , )
返回 (心跳线程句柄 ≠ 0)


.子程序 极策k_停止心跳
' 停止心跳线程
心跳停止标志 ＝ 真
' 等待线程退出（最多 3 秒）
程序_延时 (3000)


' ==================== 退出登录 ====================

.子程序 极策k_退出登录
' 退出登录，销毁 session
.局部变量 请求体, 文本型
.局部变量 响应, 文本型

极策k_停止心跳 ()

如果真 (会话ID ≠ "") Then
    请求体 ＝ "{""sessionId"":""" + 会话ID + """}"
    响应 ＝ 极策k_HTTP请求 ("POST", "/api/sdk/auth/logout", 请求体, "")
    会话ID ＝ ""


' ==================== 内部子程序 ====================

.子程序 极策k_HTTP请求, 文本型
' 构造带签名的 HTTP 请求
.参数 方法, 文本型, , "POST" 或 "GET"
.参数 路径, 文本型, , 如 /api/sdk/card/verify
.参数 请求体, 文本型
.参数 设备指纹, 文本型
.局部变量 完整URL, 文本型
.局部变量 时间戳, 文本型
.局部变量 随机串, 文本型
.局部变量 请求体哈希, 文本型
.局部变量 签名原文, 文本型
.局部变量 签名值, 文本型
.局部变量 请求头, 文本型
.局部变量 响应, 文本型

完整URL ＝ 服务端地址 + 路径
时间戳 ＝ 到文本 (取毫秒时间戳 ())
随机串 ＝ 取UUID ()

' 计算 BODY_SHA256
如果 (请求体 ＝ "") Then
    请求体哈希 ＝ ""
否则
    请求体哈希 ＝ 计算_SHA256_十六进制 (请求体)

' 签名原文：METHOD\nPATH\nTIMESTAMP\nNONCE\nBODY_SHA256
签名原文 ＝ 方法 + #换行符 + 路径 + #换行符 + 时间戳 + #换行符 + 随机串 + #换行符 + 请求体哈希

' HMAC-SHA256 → Base64
签名值 ＝ 计算_HMAC_SHA256_Base64 (签名原文, 签名密钥)

' 构造请求头（精易模块格式）
请求头 ＝ "X-App-Key: " + 应用Key + #换行符
请求头 ＝ 请求头 + "X-Timestamp: " + 时间戳 + #换行符
请求头 ＝ 请求头 + "X-Nonce: " + 随机串 + #换行符
请求头 ＝ 请求头 + "X-Signature: " + 签名值 + #换行符
请求头 ＝ 请求头 + "X-Device-Id: " + 设备指纹 + #换行符
请求头 ＝ 请求头 + "Content-Type: application/json; charset=UTF-8"

' 精易模块 HTTP 请求
响应 ＝ 精易模块.网页_访问 (完整URL, 选择 (方法 ＝ "POST", 1, 0), 请求体, 请求头, , , , , , 超时秒数 × 1000, , , )

返回 (响应)


.子程序 心跳线程入口
' 心跳线程入口（后台运行）
.局部变量 间隔, 整数型
.局部变量 退出, 逻辑型
.局部变量 下次间隔, 整数型

心跳间隔 ＝ 60
连续失败次数 ＝ 0
退出 ＝ 假

.判断循环首 (心跳停止标志 ＝ 假 且 退出 ＝ 假)
    间隔 ＝ 心跳间隔
    如果真 (连续失败次数 ＞ 0) Then
        间隔 ＝ 求次幂 (2, 连续失败次数)
        如果真 (间隔 ＞ 30) Then
            间隔 ＝ 30

    如果 (极策k_心跳 (下次间隔)) Then
        连续失败次数 ＝ 0
        极策k_心跳成功回调 ()
    否则
        连续失败次数 ＝ 连续失败次数 + 1
        极策k_心跳失败回调 (连续失败次数)
        如果真 (连续失败次数 ≥ 5) Then
            极策k_断开回调 ()
            退出 ＝ 真

    如果真 (退出 ＝ 假) Then
        程序_延时 (间隔 × 1000)
.判断循环尾 ()


' ==================== 加密子程序 ====================

.子程序 计算_SHA256_十六进制, 文本型
' SHA-256 哈希，返回 64 字符小写十六进制
.参数 数据, 文本型
.局部变量 字节集结果, 字节集

字节集结果 ＝ 精易模块.哈希_SHA256 (到字节集 (数据))
返回 (取十六进制文本 (字节集结果, ))


.子程序 计算_HMAC_SHA256_Base64, 文本型
' HMAC-SHA256 签名，返回 Base64
.参数 数据, 文本型
.参数 密钥, 文本型
.局部变量 字节集结果, 字节集

字节集结果 ＝ 精易模块.对称加密_HMAC_SHA256 (到字节集 (数据), 到字节集 (密钥))
返回 (编码_BASE64编码 (字节集结果))


.子程序 RSA_OAEP加密_Base64, 文本型
' RSA-2048-OAEP 加密（卡密传输），返回 Base64
.参数 明文, 文本型
.参数 公钥PEM, 文本型
.局部变量 加密结果, 字节集

加密结果 ＝ 精易模块.RSA_加密 (到字节集 (明文), 公钥PEM, , #RSA填充_OAEP, #哈希算法_SHA256)
返回 (编码_BASE64编码 (加密结果))


' ==================== 指纹采集 ====================

.子程序 极策k_采集设备指纹, 文本型
' 采集 5 维设备指纹，返回 JSON 字符串
.参数 RSA公钥, 文本型
.局部变量 CPU, 文本型
.局部变量 主板, 文本型
.局部变量 硬盘, 文本型
.局部变量 MAC, 文本型
.局部变量 BIOS, 文本型
.局部变量 CPU哈希, 文本型
.局部变量 主板哈希, 文本型
.局部变量 硬盘哈希, 文本型
.局部变量 MAC哈希, 文本型
.局部变量 BIOS哈希, 文本型
.局部变量 VM补充, 文本型
.局部变量 是否VM, 整数型
.局部变量 指纹输入, 文本型
.局部变量 最终指纹, 文本型
.局部变量 维度JSON, 文本型
.局部变量 加密详情, 文本型
.局部变量 返回JSON, 文本型

' 采集 5 维原始值
CPU ＝ 采集_CPU序列号 ()
主板 ＝ 采集_主板序列号 ()
硬盘 ＝ 采集_硬盘序列号 ()
MAC ＝ 采集_网卡MAC ()
BIOS ＝ 采集_BIOS序列号 ()

' 每维 SHA-256
CPU哈希 ＝ 计算_SHA256_十六进制 (CPU)
主板哈希 ＝ 计算_SHA256_十六进制 (主板)
硬盘哈希 ＝ 计算_SHA256_十六进制 (硬盘)
MAC哈希 ＝ 计算_SHA256_十六进制 (MAC)
BIOS哈希 ＝ 计算_SHA256_十六进制 (BIOS)

' VM 检测
VM补充 ＝ 检测_VM补充 ()
是否VM ＝ 选择 (VM补充 ≠ "", 1, 0)

' 拼接 → SHA-256
指纹输入 ＝ CPU哈希 + 主板哈希 + 硬盘哈希 + MAC哈希 + BIOS哈希
如果真 (VM补充 ≠ "") Then
    指纹输入 ＝ 指纹输入 + VM补充

最终指纹 ＝ 计算_SHA256_十六进制 (指纹输入)

' 5 维哈希 JSON → RSA 加密
维度JSON ＝ "{""cpu"":""" + CPU哈希 + """,""mainboard"":""" + 主板哈希 + """,""disk"":""" + 硬盘哈希 + """,""mac"":""" + MAC哈希 + """,""bios"":""" + BIOS哈希 + """}"
加密详情 ＝ RSA_OAEP加密_Base64 (维度JSON, RSA公钥)

' 构造返回 JSON
返回JSON ＝ "{""fingerprint"":""" + 最终指纹 + """,""encryptedDetail"":""" + 加密详情 + """,""isVm"":" + 到文本 (是否VM) + ",""vmExtra"":""" + VM补充 + """,""osType"":""windows"",""osVersion"":""" + 取系统版本文本 () + """,""deviceName"":""" + 取本机名 () + """,""clientVersion"":""jicek-sdk-epl-0.3.1""}"

返回 (返回JSON)


.子程序 采集_CPU序列号, 文本型
' Windows: wmic cpu get ProcessorId
.局部变量 输出, 文本型
输出 ＝ 取DOS命令输出 ("wmic cpu get ProcessorId /value")
输出 ＝ 子文本替换 (输出, "ProcessorId=", "", , , 真)
输出 ＝ 子文本替换 (输出, #换行符, "", , , 真)
输出 ＝ 删首空 (删尾空 (输出))
返回 (输出)


.子程序 采集_主板序列号, 文本型
' Windows: wmic baseboard get SerialNumber
.局部变量 输出, 文本型
输出 ＝ 取DOS命令输出 ("wmic baseboard get SerialNumber /value")
输出 ＝ 子文本替换 (输出, "SerialNumber=", "", , , 真)
输出 ＝ 子文本替换 (输出, #换行符, "", , , 真)
输出 ＝ 删首空 (删尾空 (输出))
返回 (输出)


.子程序 采集_硬盘序列号, 文本型
' Windows: wmic diskdrive get SerialNumber
.局部变量 输出, 文本型
输出 ＝ 取DOS命令输出 ("wmic diskdrive get SerialNumber /value")
输出 ＝ 子文本替换 (输出, "SerialNumber=", "", , , 真)
输出 ＝ 子文本替换 (输出, #换行符, "", , , 真)
输出 ＝ 删首空 (删尾空 (输出))
返回 (输出)


.子程序 采集_网卡MAC, 文本型
' Windows: getmac /fo csv /nh
.局部变量 输出, 文本型
.局部变量 逗号位置, 整数型

输出 ＝ 取DOS命令输出 ("getmac /fo csv /nh")
逗号位置 ＝ 寻找文本 (输出, ",", , 假)
如果 (逗号位置 ＞ 0) Then
    输出 ＝ 取文本左边 (输出, 逗号位置 - 1)
输出 ＝ 子文本替换 (输出, """", "", , , 真)
输出 ＝ 删首空 (删尾空 (输出))
返回 (输出)


.子程序 采集_BIOS序列号, 文本型
' Windows: wmic bios get SerialNumber
.局部变量 输出, 文本型
输出 ＝ 取DOS命令输出 ("wmic bios get SerialNumber /value")
输出 ＝ 子文本替换 (输出, "SerialNumber=", "", , , 真)
输出 ＝ 子文本替换 (输出, #换行符, "", , , 真)
输出 ＝ 删首空 (删尾空 (输出))
返回 (输出)


.子程序 检测_VM补充, 文本型
' 检测虚拟机/容器，返回 vm:UUID 或 container:CID 或 空
.局部变量 型号, 文本型
.局部变量 UUID, 文本型

型号 ＝ 取DOS命令输出 ("wmic computersystem get Model /value")
型号 ＝ 子文本替换 (型号, "Model=", "", , , 真)
型号 ＝ 删首空 (删尾空 (型号))

如果 (寻找文本 (型号, "Virtual", , 假) ≠ -1
    或 寻找文本 (型号, "VMware", , 假) ≠ -1
    或 寻找文本 (型号, "KVM", , 假) ≠ -1
    或 寻找文本 (型号, "Xen", , 假) ≠ -1) Then

    UUID ＝ 取DOS命令输出 ("wmic csproduct get UUID /value")
    UUID ＝ 子文本替换 (UUID, "UUID=", "", , , 真)
    UUID ＝ 删首空 (删尾空 (UUID))
    返回 ("vm:" + UUID)

返回 ("")


' ==================== JSON 工具 ====================

.子程序 JSON_取字段, 文本型
' 从 JSON 字符串提取字段值（简化实现，不支持嵌套）
' 推荐：使用精易模块的 JSON_解析 或 JSON_取字段 函数
.参数 JSON文本, 文本型
.参数 字段名, 文本型
.局部变量 正则, 正则表达式
.局部变量 匹配结果, 文本型

' 使用精易模块的正则
正则.创建 ("""字段名""\s*:\s*""([^""]*)""", 真)
匹配结果 ＝ 正则.取匹配文本 (JSON文本, 1)
返回 (匹配结果)


' ==================== 回调（用户重写） ====================

.子程序 极策k_心跳成功回调
' 心跳成功时调用（用户可重写）
调试输出 ("[极策k] 心跳成功")


.子程序 极策k_心跳失败回调
' 心跳失败时调用（用户可重写）
.参数 失败次数, 整数型
调试输出 ("[极策k] 心跳失败，第 " + 到文本 (失败次数) + " 次")


.子程序 极策k_断开回调
' 连续 5 次失败，断开时调用（用户可重写）
调试输出 ("[极策k] 心跳断开，已停止")


.子程序 极策k_设备封禁回调
' 设备被封禁时调用（用户可重写）
调试输出 ("[极策k] 设备已封禁")


.子程序 极策k_处理错误码
' 错误码统一处理
.参数 错误码, 整数型
.参数 错误消息, 文本型

.判断开始
    .判断 (错误码 ＝ 401)
        调试输出 ("[极策k] 未登录")
    .判断 (错误码 ＝ 1001)
        调试输出 ("[极策k] 卡密不存在")
    .判断 (错误码 ＝ 1002)
        调试输出 ("[极策k] 卡密已使用")
    .判断 (错误码 ＝ 1003)
        调试输出 ("[极策k] 卡密已封禁")
    .判断 (错误码 ＝ 1004)
        调试输出 ("[极策k] 卡密已过期")
    .判断 (错误码 ＝ 3002)
        极策k_设备封禁回调 ()
    .判断 (错误码 ＝ 3005)
        极策k_断开回调 ()
    .默认
        调试输出 ("[极策k] 错误 [" + 到文本 (错误码) + "]: " + 错误消息)
.判断结束
